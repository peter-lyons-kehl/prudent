**Not** full tests. Primary purpose: to run with `cargo expand`, so you can check the output of `prudent` macros.

See also documentation comments for `unsafe_fn` in [prudent's src/lib.rs](../src/lib.rs).
